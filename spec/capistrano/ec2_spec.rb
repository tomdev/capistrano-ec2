require 'spec_helper'

describe Capistrano::Ec2 do
  it 'has a version number' do
    expect(Capistrano::Ec2::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
